# Alamat HTTPS yang dapat dibuka dari ponsel siswa.
# Wajib untuk fitur kamera karena browser memblokir kamera pada HTTP biasa.
PUBLIC_BASE_URL=https://absensi.sekolah.example

# Lokasi executable Chromium atau Google Chrome.
# Linux:
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
# Windows (hapus tanda # untuk memakai nilai ini):
# PUPPETEER_EXECUTABLE_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe

# Lokasi database SQLite. Biarkan nilai ini untuk penyimpanan standar.
DB_PATH=./data/absensi.sqlite

# Pengaturan antrean dan worker verifikasi wajah.
FACE_WORKER_COUNT=2
FACE_QUEUE_LIMIT=100
FACE_TIMEOUT_MS=60000

# Pengaturan pengiriman notifikasi WhatsApp.
NOTIFICATION_CONCURRENCY=2
NOTIFICATION_QUEUE_LIMIT=200
LID_LOOKUP_TIMEOUT_MS=1500
